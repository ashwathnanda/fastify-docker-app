version : "3.9"
services:
  fastify-node-app:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - .:/app:ro
      - /app/node_modules
    env_file:
      - config/dev/.env
    command: npm run start-dev